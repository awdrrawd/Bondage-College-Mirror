import { ActivityManager as _ActivityManager } from "@sugarch/bc-activity-manager";

/** @type {import("@sugarch/bc-activity-manager").ActivityManagerType<string, CustomActivityPrerequisite>} */
export const ActivityManager = _ActivityManager;
